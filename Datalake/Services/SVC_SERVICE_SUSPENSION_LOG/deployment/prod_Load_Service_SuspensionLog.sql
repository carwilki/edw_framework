-- Databricks notebook source
--Reset
--TRUNCATE TABLE legacy.svc_service_suspension_log
--TRUNCATE TABLE legacy.SVC_SUSPENSION_SUBMITTER_TITLE
--TRUNCATE TABLE legacy.SVC_SERVICE_AREA
--TRUNCATE TABLE legacy.SVC_SERVICE_SUSPENSION_REASON

-- COMMAND ----------

INSERT INTO legacy.svc_service_suspension_log(DAY_DT,
  LOCATION_ID,
  SERVICE_AREA_ID,
  SUSPENSION_REASON_ID,
  SUBMITTED_BY,
  SUBMITTED_BY_POSITION_ID,
  COMMENTS,
  REVERSAL_TSTMP,
  CREATE_TSTMP,
  UPDATE_TSTMP,
  LOAD_TSTMP)
select 
  DAY_DT,
  LOCATION_ID,
  SERVICE_AREA_ID,
  SUSPENSION_REASON_ID,
  SUBMITTED_BY,
  SUBMITTED_BY_POSITION_ID,
  COMMENTS,
  REVERSAL_TSTMP,
  CREATE_TSTMP,
  UPDATE_TSTMP,
  LOAD_TSTMP
  FROM qa_legacy.svc_service_suspension_log;

-- COMMAND ----------

INSERT INTO legacy.SVC_SUSPENSION_SUBMITTER_TITLE(SUBMITTED_BY_POSITION_ID, SUBMITTED_BY_POSITION_DESC)
select SUBMITTED_BY_POSITION_ID, SUBMITTED_BY_POSITION_DESC from qa_legacy.SVC_SUSPENSION_SUBMITTER_TITLE;

-- COMMAND ----------

INSERT INTO legacy.SVC_SERVICE_AREA(SERVICE_AREA_ID, SERVICE_AREA_DESC)
SELECT SERVICE_AREA_ID, SERVICE_AREA_DESC from qa_legacy.SVC_SERVICE_AREA;

-- COMMAND ----------

INSERT INTO legacy.SVC_SERVICE_SUSPENSION_REASON(SUSPENSION_REASON_ID, SUSPENSION_REASON_DESC)
select SUSPENSION_REASON_ID, SUSPENSION_REASON_DESC from qa_legacy.SVC_SERVICE_SUSPENSION_REASON;
