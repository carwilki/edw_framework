"""this file contains the metadata for the cdc_metadata table in the database"""

# defines the catalog for the cdc_metadata table
cdc_metadata_catalog = None
# defines the schema for the cdc_metadata table
cdc_metadata_schema = "stranger_things"
# defines the table for the cdc_metadata table
cdc_metadata_table = "snowflake_cdc_log"
